name: Check
on:
  - push
  - pull_request
jobs:
  check:
    strategy:
      fail-fast: false
      matrix:
        os:
          - macos
          - ubuntu
          - windows
        R:
          - "devel"
          - "4.0"
        exclude:
          - os: macos
            R: "devel"
          - os: ubuntu
            R: "devel"
    name: R ${{ matrix.R }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}-latest
    steps:
      - uses: actions/checkout@master
      - uses: r-lib/actions/setup-r@v1
        with:
          r-version: ${{ matrix.R }}
      - uses: r-lib/actions/setup-pandoc@v1
      - uses: r-lib/actions/setup-tinytex@v1
      - name: Show R version
        if: |
          matrix.os == 'windows'
        run: |
          R.exe --version
      - name: Install system packages
        if: |
          matrix.os == 'ubuntu'
        run: |
          sudo apt update
          sudo apt install -V -y \
            libcurl4-openssl-dev \
            pandoc-citeproc \
            qpdf
      - name: Install remotes
        run: |
          Rscript -e "install.packages(c('remotes'))"
      - name: Install rcmdcheck
        run: |
          Rscript -e "remotes::install_cran(c('rcmdcheck'))"
      - name: Install roxygen2
        run: |
          Rscript -e "remotes::install_cran(c('roxygen2'))"
      - name: Install BiocManager
        run: |
          Rscript -e "install.packages(c('BiocManager'))"
          Rscript -e "BiocManager::install()"
      - name: Install multtest separately
        run: |
          Rscript -e "BiocManager::install(c('multtest'))"
      - name: Install dependencies
        run: |
          Rscript -e "remotes::install_deps(dependencies=TRUE)"
      - name: Run roxygen
        run: |
          Rscript -e "roxygen2::roxygenise()"
      - name: "Check: not accept warning"
        if: |
          matrix.os != 'windows'
        run: |
          Rscript -e "rcmdcheck::rcmdcheck(args=c('--as-cran'), error_on='warning', check_dir='check')"
      - name: "Check: accept warning"
        if: |
          matrix.os == 'windows'
        run: |
          Rscript -e "rcmdcheck::rcmdcheck(args=c('--as-cran'), check_dir='check')"
      - name: "check/treefit.Rcheck/Rdlatex.log"
        if: |
          always()
        continue-on-error: true
        run: |
          cat check/treefit.Rcheck/Rdlatex.log
      - name: "check/treefit.Rcheck/treefit-manual.log"
        if: |
          always()
        continue-on-error: true
        run: |
          cat check/treefit.Rcheck/treefit-manual.log
      - name: Debug
        if: |
          matrix.os == 'windows'
        continue-on-error: true
        shell: bash
        run: |
          find check
      - name: Re-run Rd2pdf
        if: |
          matrix.os == 'windows'
        continue-on-error: true
        run: |
          R.exe CMD Rd2pdf check\treefit.Rcheck\00_pkg_src\treefit\man\*.Rd
